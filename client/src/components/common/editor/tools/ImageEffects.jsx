// import { ImageContainer } from "image-effects-react"

export function ImageEffects({ imageUrl }) {

    return (
        <div>
            {/* <ImageContainer
                options={{
                    filter: '1977',
                }}
            >
                <img src={imageUrl} alt="filter" />
            </ImageContainer> */}
        </div>
    )
}